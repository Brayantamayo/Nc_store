/**
 * Módulo: Categoría
 * Servicio API — En proceso
 */
export const categoriaService = {
  listar: async () => {
    throw new Error('En proceso — categoriaService.listar');
  },
  obtenerPorId: async (_id: string) => {
    throw new Error('En proceso — categoriaService.obtenerPorId');
  },
  crear: async () => {
    throw new Error('En proceso — categoriaService.crear');
  },
  actualizar: async (_id: string) => {
    throw new Error('En proceso — categoriaService.actualizar');
  },
  eliminar: async (_id: string) => {
    throw new Error('En proceso — categoriaService.eliminar');
  },
};
