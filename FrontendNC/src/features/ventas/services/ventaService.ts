/**
 * Módulo: Ventas
 * Servicio API — En proceso
 */
export const ventaService = {
  listar: async () => {
    throw new Error('En proceso — ventaService.listar');
  },
  obtenerPorId: async (_id: string) => {
    throw new Error('En proceso — ventaService.obtenerPorId');
  },
  crear: async () => {
    throw new Error('En proceso — ventaService.crear');
  },
  actualizar: async (_id: string) => {
    throw new Error('En proceso — ventaService.actualizar');
  },
  eliminar: async (_id: string) => {
    throw new Error('En proceso — ventaService.eliminar');
  },
};
