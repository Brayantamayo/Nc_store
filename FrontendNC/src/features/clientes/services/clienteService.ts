/**
 * Módulo: Clientes
 * Servicio API — En proceso
 */
export const clienteService = {
  listar: async () => {
    throw new Error('En proceso — clienteService.listar');
  },
  obtenerPorId: async (_id: string) => {
    throw new Error('En proceso — clienteService.obtenerPorId');
  },
  crear: async () => {
    throw new Error('En proceso — clienteService.crear');
  },
  actualizar: async (_id: string) => {
    throw new Error('En proceso — clienteService.actualizar');
  },
  eliminar: async (_id: string) => {
    throw new Error('En proceso — clienteService.eliminar');
  },
};
