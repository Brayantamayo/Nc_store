/**
 * Módulo: Pedidos
 * Compras ya pagadas — listado
 * Componente: Listar (tabla)
 * Estado: En proceso
 */
export const PedidosTable = () => {
  return <p>En proceso — Listado de pedidos (compras pagadas)</p>;
};
