/**
 * Módulo: Favoritos
 * Servicio API — En proceso
 */
export const favoritoService = {
  listar: async () => {
    throw new Error('En proceso — favoritoService.listar');
  },
  obtenerPorId: async (_id: string) => {
    throw new Error('En proceso — favoritoService.obtenerPorId');
  },
  crear: async () => {
    throw new Error('En proceso — favoritoService.crear');
  },
  eliminar: async (_id: string) => {
    throw new Error('En proceso — favoritoService.eliminar');
  },
};
