/**
 * Módulo: Pagos
 * Servicio API — En proceso
 */
export const pagoService = {
  listar: async () => {
    throw new Error('En proceso — pagoService.listar');
  },
  obtenerPorId: async (_id: string) => {
    throw new Error('En proceso — pagoService.obtenerPorId');
  },
  crear: async () => {
    throw new Error('En proceso — pagoService.crear');
  },
  actualizar: async (_id: string) => {
    throw new Error('En proceso — pagoService.actualizar');
  },
  eliminar: async (_id: string) => {
    throw new Error('En proceso — pagoService.eliminar');
  },
};
